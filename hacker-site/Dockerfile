# Dockerfile para sitio del Hacker (nginx)
FROM nginx:alpine

# Copiar el HTML malicioso al directorio de nginx
COPY index.html /usr/share/nginx/html/

# Exponer puerto 80
EXPOSE 80

# nginx se inicia automáticamente
